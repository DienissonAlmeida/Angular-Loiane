var minhaVar = 'minha variavel';

function minhaFunc(x, y){
    return x + y;
}

let num = 2;
const pi = 3.14;

var numeros = [1,2,3];

numeros.map(value => value * 2);

class Matematica {

    soma(x:number,y:number): number {
        return x + y;
    }
}
var n1: string = 'asdasda';
